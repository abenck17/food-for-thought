<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>All Recipes</title>
        <link rel="stylesheet" href="/css/app2.css">
    </head>
    <body>
    <div class="flex">
        <h1>All Recipes</h1>
    
        <ul class="recipeList">
            <% for(let i=0; i<recipes.length; i++) { %>
                <li>
                    <nav class="singleRecipe">
                        <a href="<%=recipes[i].link%>"><%=recipes[i].name%></a>
                        <a>Calorie (<%=recipes[i].calorie%>)<br/></a>
                        <a>Fat (<%=recipes[i].fat%>g)<br/></a>
                        <a>Carb (<%=recipes[i].carb%>g)<br/></a>
                        <a>Protein (<%=recipes[i].protein%>g)<br/></a>
                    </nav>
                </br>
                </br>
                    <!-- Add edit link here  -->
			        <a href="/recipes/<%=recipes[i].id %>/edit"><button type="button">Edit</button></a>
                </br>
                </br>
                    <!--  ADD DELETE FORM HERE-->
			        <form action="/recipes/<%=recipes[i].id %>?_method=DELETE" method="POST">
				        <input type="submit" value="DELETE"/>
                    </form>
                </br>
                </li>
            <% } %>
        </ul>
    </div>
    </body>
</html>